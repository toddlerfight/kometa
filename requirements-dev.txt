# Dev/test-only deps. NOT installed in the Docker image — keep this out of the
# runtime requirements.txt so it never ships to the NAS container.
-r requirements.txt
pytest==9.0.3
# Browser smoke suite (tests/e2e, opt-in via `pytest -m e2e`). After install:
#   .venv/bin/python -m playwright install chromium
pytest-playwright==0.8.0
playwright==1.61.0
# External audit tools (2026-07-03) — run in the pre-deploy check:
#   pip-audit            → CVEs in the dependency tree
#   bandit -r kometa/ -c pyproject.toml → security AST lint
#   deptry kometa/       → dead/missing/misplaced deps
pip-audit==2.10.0
bandit==1.9.4
deptry==0.25.1
